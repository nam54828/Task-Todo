import 'package:flutter/material.dart';

class crud_List extends StatelessWidget {
  const crud_List({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

